# Use an official Node.js runtime as a parent image
FROM node:18-alpine

# Set the working directory inside the container
WORKDIR /app

# Copy the frontend's package.json and package-lock.json
COPY package*.json ./

# Install the frontend dependencies
RUN npm install

# Copy the rest of your frontend source code
COPY . .

# The command to run your Vite development server
# The --host flag is crucial to make the server accessible from outside the container
CMD ["npm", "run", "dev", "--", "--host"]